# SSR Demo

## Install

```
npm install
```

## Generage ssr.php

Generate ssr file in PHP (`dist/index.php`):

```bash
npm run build   // or `npm run jsbuild`
```

## Render

Pass `data.json` to `ssr.php`:

```bash
npm run render
```
